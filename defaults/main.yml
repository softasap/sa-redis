---
# defaults file for sa-redis

  redis_bind_to: 127.0.0.1

  redis_properties:
    - {regexp: "^bind *", line: "bind {{redis_bind_to}}"}
#   - {regexp: "^requirepass *", line:"requirepass YOUR_SECURE_PASS"}  # Secure redis with pass

  option_install_redis_commander: false
  option_install_using_chris_lea_ppa: false
